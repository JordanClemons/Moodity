import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({

      circleText:{
        textAlign: 'center',
        color: '#F3DD6D',
        fontSize: 80,
      },

      blankText:{
        fontSize: 22,
        color: "#FFF"
      }

})

export default styles;